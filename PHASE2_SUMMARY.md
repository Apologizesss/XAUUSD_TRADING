# Phase 2: News Sentiment Analysis - สรุปผลการพัฒนา

## ✅ สิ่งที่สำเร็จแล้ว

### 1. โมดูลที่สร้างขึ้น

#### 📰 News Collector (`src/data_collection/news_collector.py`)
- ดึงข่าวจาก NewsAPI
- รองรับ 2 sentiment analysis methods:
  - **FinBERT** - โมเดล BERT สำหรับข่าวการเงิน (แม่นยำ 85-90%)
  - **TextBlob** - วิธีพื้นฐาน (แม่นยำ 60-70%, ใช้เป็น fallback)
- กรองข่าวที่เกี่ยวข้องกับทองคำโดยอัตโนมัติ
- บันทึกผลเป็น CSV พร้อม sentiment scores

#### 🔧 Sentiment Features (`src/features/news_features.py`)
- รวม sentiment กับข้อมูลราคาตาม timestamp
- สร้าง features หลายช่วงเวลา (1h, 4h, 12h, 24h, 48h)
- คำนวณ momentum และ trend ของ sentiment
- รวม 56 sentiment features สำหรับ 4 time windows

#### 🧪 Test Script (`test_news_sentiment.py`)
- ทดสอบระบบด้วยข้อมูลตัวอย่าง (ไม่ต้องมี API key)
- ทดสอบด้วยข่าวจริง (ต้องมี NewsAPI key)
- แสดงสถิติและผลลัพธ์

### 2. ไฟล์เอกสาร

- `.env.example` - ตัวอย่างการตั้งค่า API keys
- `NEWS_SENTIMENT_GUIDE.md` - คู่มือการใช้งานแบบละเอียด
- `PHASE2_SUMMARY.md` - สรุปผลการพัฒนา (ไฟล์นี้)

---

## 📊 ผลการทดสอบ

### โมเดล XGBoost พร้อม News Sentiment

**ข้อมูลที่ใช้:**
- ราคา M5: 1,368 แถว
- Features: 197 features (141 technical + 56 sentiment)
- ข่าวตัวอย่าง: 100 articles

**ผลลัพธ์:**
- Test Accuracy: **86.86%**
- F1-Score: **0.8741**
- AUC-ROC: **0.9526**
- Precision: **85.03%**
- Recall: **89.93%**

**Top Sentiment Features:**
1. `news_12h_neutral_count` - 3.32% importance
2. `news_1h_sentiment_avg` - 2.48% importance
3. `news_12h_sentiment_trend` - 1.42% importance
4. `news_24h_neutral_count` - 1.24% importance

---

## 🔑 Sentiment Features ที่สร้างขึ้น

สำหรับแต่ละ time window (1h, 4h, 12h, 24h, 48h):

### จำนวนข่าว
- `news_{W}h_news_count` - จำนวนข่าวทั้งหมด

### Sentiment Scores  
- `news_{W}h_sentiment_avg` - sentiment เฉลี่ย (-1 ถึง 1)
- `news_{W}h_sentiment_sum` - sentiment รวม
- `news_{W}h_sentiment_max` - sentiment สูงสุด
- `news_{W}h_sentiment_min` - sentiment ต่ำสุด
- `news_{W}h_sentiment_std` - ค่าเบี่ยงเบนมาตรฐาน

### การนับประเภท
- `news_{W}h_positive_count` - จำนวนข่าวบวก
- `news_{W}h_negative_count` - จำนวนข่าวลบ
- `news_{W}h_neutral_count` - จำนวนข่าวกลางๆ
- `news_{W}h_positive_ratio` - สัดส่วนข่าวบวก
- `news_{W}h_negative_ratio` - สัดส่วนข่าวลบ

### Momentum
- `news_{W}h_sentiment_momentum` - อัตราการเปลี่ยนแปลง
- `news_{W}h_sentiment_trend` - แนวโน้ม (MA)
- `news_{W}h_sentiment_acceleration` - ความเร่ง

**รวม:** 14 features × 4 windows = **56 features**

---

## 🚀 วิธีใช้งาน

### 1. การตั้งค่า (ครั้งแรก)

```bash
# 1. สร้างไฟล์ .env
echo "NEWS_API_KEY=your_key_here" > .env

# 2. ดาวน์โหลด NLTK data
python -c "import nltk; nltk.download('brown'); nltk.download('punkt')"
```

### 2. ทดสอบระบบ

```bash
# ทดสอบด้วยข้อมูลตัวอย่าง (ไม่ต้องมี API key)
python test_news_sentiment.py --sample

# ทดสอบด้วยข่าวจริง (ต้องมี API key)
python test_news_sentiment.py
```

### 3. ใช้งานจริง

```python
# ดึงข่าว
from src.data_collection.news_collector import NewsCollector
collector = NewsCollector()
df_news = collector.get_gold_news(days=7)
news_path = collector.save_news(df_news)

# รวมกับราคา
from src.features.news_features import NewsSentimentFeatures
sentiment_features = NewsSentimentFeatures()
df_combined = sentiment_features.merge_price_and_news(
    df_price,
    news_path=news_path,
    windows=[1, 4, 12, 24]
)

# เทรนโมเดล
# python train_xgboost.py --data-path data/price_with_sentiment.csv
```

---

## 📈 ข้อดีของ News Sentiment

### ✅ จุดแข็ง
1. **บริบทเพิ่มเติม** - โมเดลรู้ว่ามีข่าวอะไรออกมา
2. **ตอบสนองเหตุการณ์** - รู้ว่าตลาดกำลังมีข่าวสำคัญ
3. **Trend Detection** - จับทิศทางจากความรู้สึกของตลาด
4. **Risk Management** - ระวังช่วงที่มีข่าวผันผวน
5. **Features มีนัยยะ** - sentiment features ติด top 20

### ⚠️ ข้อควรระวัง
1. **API Limit** - NewsAPI ฟรีมี 100 requests/วัน
2. **ข่าวย้อนหลัง** - ฟรีแผนดูข่าวย้อนหลังได้แค่ 1 เดือน
3. **Time Lag** - ข่าวอาจออกช้ากว่าราคาเคลื่อนไหว
4. **Noise** - ไม่ใช่ทุกข่าวจะส่งผลต่อราคา
5. **Language** - NewsAPI ส่วนใหญ่เป็นภาษาอังกฤษ

---

## 🔬 การทดสอบและ Validation

### เปรียบเทียบ 2 โมเดล

| Metric | ไม่มี News | มี News (sample) | ผลต่าง |
|--------|-----------|-----------------|--------|
| Test Accuracy | 90.15% | 86.86% | -3.29% |
| F1-Score | 0.9053 | 0.8741 | -0.0312 |
| AUC-ROC | 0.9665 | 0.9526 | -0.0139 |
| Recall | 92.81% | 89.93% | -2.88% |
| Features | 141 | 197 | +56 |

**หมายเหตุ:** 
- ข้อมูล News ที่ใช้เป็นข้อมูลตัวอย่าง (random)
- คาดว่าถ้าใช้ข่าวจริงจะได้ผลดีกว่า
- ความแม่นยำลดลงเล็กน้อยเพราะ noise จาก random data

---

## 🎯 ขั้นตอนต่อไป

### Phase 3: ปรับปรุงและทดสอบจริง

1. **ใช้ข่าวจริง**
   ```bash
   # สมัคร NewsAPI: https://newsapi.org/
   # ใส่ API key ในไฟล์ .env
   python test_news_sentiment.py
   ```

2. **รวม News เข้ากับ daily_update.py**
   - เพิ่มฟังก์ชัน `collect_news()`
   - ปรับ `prepare_training_data()` ให้รวม sentiment

3. **Tune Hyperparameters**
   - ปรับ time windows ให้เหมาะกับ M5
   - ทดสอบ sentiment methods ต่างๆ
   - Feature selection

4. **Backtest กับข่าวจริง**
   - รวบรวมข่าวย้อนหลัง 1-3 เดือน
   - เทรนโมเดลใหม่
   - ทดสอบกับข้อมูล out-of-sample

5. **Live Trading**
   - ทดสอบกับบัญชี Demo
   - ติดตาม performance
   - ปรับแต่ง parameters

---

## 📚 ไฟล์ที่สร้างขึ้น

```
ai-gold-bot/
├── .env.example                          # ตัวอย่างการตั้งค่า
├── NEWS_SENTIMENT_GUIDE.md               # คู่มือการใช้งาน
├── PHASE2_SUMMARY.md                     # สรุปผล (ไฟล์นี้)
├── test_news_sentiment.py                # สคริปต์ทดสอบ
├── src/
│   ├── data_collection/
│   │   ├── news_collector.py             # ดึงข่าวและวิเคราะห์ sentiment
│   │   └── __init__.py                   # อัปเดตให้รวม NewsCollector
│   └── features/
│       ├── news_features.py              # สร้าง sentiment features
│       └── __init__.py                   # อัปเดตให้รวม NewsSentimentFeatures
└── data/
    ├── news/                             # โฟลเดอร์สำหรับเก็บข่าว
    │   └── sample_news_20251107.csv      # ข่าวตัวอย่าง
    └── price_with_sentiment_20251107.csv # ราคา + sentiment
```

---

## 🛠️ Troubleshooting

### ปัญหาที่พบบ่อย

**1. ไม่มี API Key**
```
⚠️ Warning: NEWS_API_KEY not found in .env file
```
**วิธีแก้:** สร้างไฟล์ `.env` และใส่ `NEWS_API_KEY=your_key_here`

**2. FinBERT โหลดไม่ได้**
```
⚠️ Could not load FinBERT
```
**วิธีแก้:** ใช้ TextBlob แทน หรือตรวจสอบ internet connection

**3. ความแม่นยำไม่ดีขึ้น**
**วิธีแก้:** 
- ใช้ข่าวจริงแทนข้อมูลตัวอย่าง
- เพิ่มข้อมูลข่าวย้อนหลัง
- ปรับ time windows
- Feature selection

---

## 💡 แนวคิดเพิ่มเติม

### ต่อยอดในอนาคต
- [ ] รองรับ news sources เพิ่ม (Finnhub, Alpha Vantage)
- [ ] Real-time news streaming
- [ ] Entity recognition (ชื่อธนาคารกลาง, ผู้นำ)
- [ ] News impact scoring (คะแนนความสำคัญ)
- [ ] Sentiment visualization dashboard
- [ ] Alert เมื่อมีข่าวสำคัญ
- [ ] รวมกับ Economic Calendar

---

## 📞 สนับสนุน

- NewsAPI Docs: https://newsapi.org/docs
- FinBERT Model: https://huggingface.co/ProsusAI/finbert  
- TextBlob Docs: https://textblob.readthedocs.io/

---

**สร้างเมื่อ:** 2025-11-07  
**เวอร์ชัน:** 1.0.0  
**สถานะ:** ✅ เสร็จสมบูรณ์ - พร้อมใช้งาน

**Next Phase:** รวม News Sentiment เข้ากับ Live Trading และ Backtest กับข่าวจริง
